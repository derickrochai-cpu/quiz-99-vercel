========================================
DEPLOY DO QUIZ 99 - COMANDOS RÁPIDOS
========================================

🚀 PASSO A PASSO PARA DEPLOY

1. INSTALAR VERCEL CLI (apenas primeira vez)
-------------------------------------------
npm i -g vercel

2. FAZER LOGIN NA VERCEL
-------------------------
vercel login

3. FAZER DEPLOY
----------------
cd C:\Users\derickrocha_i\quiz-99-vercel
vercel --prod


========================================
CONFIGURAÇÃO DO SUPABASE (ANTES DO DEPLOY)
========================================

1. Criar projeto em: https://supabase.com

2. Executar SQL no Editor SQL:
-------------------------------
CREATE TABLE games (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT UNIQUE NOT NULL,
  title TEXT NOT NULL,
  questions JSONB NOT NULL DEFAULT '[]',
  status TEXT NOT NULL DEFAULT 'waiting',
  current_question INTEGER DEFAULT -1,
  players JSONB DEFAULT '[]',
  answers JSONB DEFAULT '{}',
  time_left INTEGER,
  started_at TIMESTAMP WITH TIME ZONE,
  question_started_at TIMESTAMP WITH TIME ZONE,
  results JSONB,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_games_code ON games(code);
CREATE INDEX idx_games_status ON games(status);

3. Copiar credenciais em: Project Settings → API
   - SUPABASE_URL: https://xxxxx.supabase.co
   - SUPABASE_ANON_KEY: eyJ...


========================================
VARIÁVEIS DE AMBIENTE NA VERCEL
========================================

Acesse: https://vercel.com/dashboard
→ Selecione o projeto → Settings → Environment Variables

Adicione estas variáveis:

Name: SUPABASE_URL
Value: https://seu-projeto.supabase.co

Name: SUPABASE_ANON_KEY
Value: eyJ...

Name: JWT_SECRET
Value: (use uma senha aleatória forte)

Name: ADMIN_EMAIL
Value: seu-email@exemplo.com

Name: ADMIN_PASSWORD
Value: (senha segura para o admin)


========================================
COMANDOS ÚTEIS
========================================

# Verificar se está tudo ok
node check-deploy.js

# Deploy para preview
vercel

# Deploy para produção
vercel --prod

# Ver logs
vercel logs --production

# Ver logs em tempo real
vercel logs -f


========================================
URL APÓS O DEPLOY
========================================

https://seu-projeto.vercel.app

========================================
